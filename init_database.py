#!/usr/bin/env python3
"""
Скрипт для инициализации базы данных для Flask-приложения.

Этот скрипт импортирует экземпляр приложения Flask и объект SQLAlchemy,
создает контекст приложения и затем вызывает db.create_all() для создания
всех таблиц на основе определенных моделей.
"""

import os
import sys
import logging
from dotenv import load_dotenv

# --- Начальная настройка ---

# 1. Добавляем корневую папку проекта (где находится папка 'src') в путь Python.
#    Это необходимо, чтобы Python мог найти модуль 'src' и импортировать из него.
#    os.path.dirname(__file__) -> текущая директория (telegram-hr-bot)
#    os.path.join(...) -> telegram-hr-bot/src
sys.path.append(os.path.join(os.path.dirname(__file__), 'src'))

# 2. Загружаем переменные окружения из файла .env
#    Это важно, так как конфигурация базы данных (URI) обычно хранится там.
load_dotenv()

# 3. Настраиваем логирование для вывода информативных сообщений.
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s'
)
logger = logging.getLogger(__name__)


def initialize_database():
    """
    Основная функция для инициализации базы данных.
    """
    logger.info("🚀 Запуск скрипта инициализации базы данных...")
    
    try:
        # --- Импорт приложения ---
        # Пытаемся импортировать 'app' (экземпляр Flask) и 'db' (экземпляр SQLAlchemy)
        # из вашего главного файла приложения, который, судя по всему, 'src/main.py'.
        logger.info("Импортируем 'app' и 'db' из 'src.main'...")
        from main import app, db
        logger.info("Импорт успешно завершен.")

        # --- Создание контекста приложения (КЛЮЧЕВОЕ ИСПРАВЛЕНИЕ) ---
        # Любые операции, которые зависят от конфигурации Flask или его расширений (как SQLAlchemy),
        # должны выполняться внутри "контекста приложения".
        # Конструкция 'with app.app_context():' создает этот контекст на время выполнения блока.
        with app.app_context():
            logger.info("Вход в контекст приложения Flask.")
            
            # Получаем URI базы данных из конфигурации, чтобы убедиться, что все настроено верно.
            db_uri = app.config.get('SQLALCHEMY_DATABASE_URI')
            logger.info(f"Используется URI базы данных: {db_uri}")

            # --- Создание таблиц ---
            # db.create_all() - это команда Flask-SQLAlchemy.
            # Она автоматически находит все классы в вашем коде, которые наследуются от db.Model,
            # и создает для них соответствующие таблицы в базе данных.
            logger.info("Выполняется команда db.create_all() для создания таблиц...")
            db.create_all()
            
            logger.info("✅ Команда db.create_all() успешно выполнена.")

    except ImportError as e:
        logger.error(f"❌ КРИТИЧЕСКАЯ ОШИБКА: Не удалось импортировать 'app' или 'db'.")
        logger.error(f"   Проверьте, что файл 'src/main.py' существует и содержит переменные 'app' и 'db'.")
        logger.error(f"   Текст системной ошибки: {e}")
        sys.exit(1) # Выход из скрипта с кодом ошибки

    except Exception as e:
        logger.error(f"❌ Произошла непредвиденная ошибка во время инициализации БД.")
        # exc_info=True добавляет полную трассировку ошибки в лог для детальной диагностики.
        logger.error(f"   Текст системной ошибки: {e}", exc_info=True)
        sys.exit(1) # Выход из скрипта с кодом ошибки

    logger.info("🎉 Инициализация базы данных успешно завершена!")


# --- Точка входа в скрипт ---
# Эта стандартная конструкция Python означает: "если этот файл запускается напрямую
# (а не импортируется как модуль), то выполнить функцию initialize_database()".
if __name__ == "__main__":
    initialize_database()
