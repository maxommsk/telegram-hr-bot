#!/usr/bin/env python3
"""
Скрипт для инициализации базы данных для Flask-приложения.
"""

import os
import sys
import logging
from dotenv import load_dotenv

# --- Начальная настройка ---

# Добавляем корневую папку проекта в путь Python.
# Это нужно, чтобы Python мог находить пакеты внутри папки 'src'.
# os.path.dirname(__file__) вернет путь к папке, где лежит init_database.py.
sys.path.append(os.path.dirname(__file__))

# Загружаем переменные окружения из файла .env (например, строку подключения к БД).
load_dotenv()

# Настраиваем систему логирования для вывода понятных сообщений о ходе выполнения.
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s'
)
logger = logging.getLogger(__name__)


def initialize_database():
    """
    Основная функция для инициализации базы данных.
    """
    logger.info("🚀 Запуск скрипта инициализации базы данных...")
    
    try:
        # --- Точечный импорт, чтобы обойти возможные циклические зависимости ---
        # Мы импортируем нужные нам компоненты напрямую из их файлов,
        # чтобы избежать выполнения кода в промежуточных __init__.py, который может вызывать ошибки.
        
        logger.info("Импортируем 'app' (экземпляр Flask) и 'db' (объект SQLAlchemy) из 'src.main'...")
        from src.main import app, db
        logger.info("Импорт 'app' и 'db' успешно завершен.")

        logger.info("Принудительно импортируем все классы моделей из 'src.models'...")
        from src.models import *
        logger.info("Все известные модели импортированы. SQLAlchemy теперь знает, какие таблицы создавать.")

        # --- Создание контекста приложения и таблиц ---
        # Любые операции с базой данных в Flask-SQLAlchemy требуют "контекста приложения".
        # Конструкция 'with app.app_context():' предоставляет этот контекст.
        with app.app_context():
            logger.info("Вход в контекст приложения Flask.")
            
            # Выводим URI базы данных для проверки, что конфигурация загрузилась правильно.
            db_uri = app.config.get('SQLALCHEMY_DATABASE_URI')
            logger.info(f"Используется URI базы данных: {db_uri}")

            # Главная команда: создает все таблицы, для которых были найдены модели.
            logger.info("Выполняется команда db.create_all() для создания таблиц...")
            db.create_all()
            
            logger.info("✅ Команда db.create_all() успешно выполнена.")

    except Exception as e:
        # Отлавливаем абсолютно любую ошибку, чтобы получить максимум информации.
        logger.error(f"❌ Произошла непредвиденная ошибка во время инициализации БД.")
        # exc_info=True добавляет в лог полную трассировку ошибки, что очень важно для диагностики.
        logger.error(f"   Текст системной ошибки: {e}", exc_info=True)
        sys.exit(1) # Завершаем скрипт с кодом ошибки, чтобы показать, что он не был успешным.

    logger.info("🎉 Инициализация базы данных успешно завершена!")


# Стандартная точка входа в Python-скрипт.
if __name__ == "__main__":
    initialize_database()
